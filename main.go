package main

import (
	"fmt"
)

func main() {

	var choice int
	fmt.Scanf("%d", &choice)
	if choice == 1 {
		fmt.Println(choice)
	} else if choice == 2 {
		fmt.Println(choice)
	} else {
		fmt.Println(choice)
	}
}
